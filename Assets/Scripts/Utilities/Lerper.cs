using System;

namespace Utilities
{
    public static class Lerper
    {
        public static void Lerp<T>(T from, T to, float time, Action<T, T, float> update)
        {
            
        }
    }
}